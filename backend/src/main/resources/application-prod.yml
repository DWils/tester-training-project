spring:
    datasource:
        url: jdbc:postgresql://${DB_SERVER:localhost:5432}/databasenameprod
        username: postgresprod
        password: adminprod
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: false
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
