spring:
  application:
    name: training-test-project-api
  datasource:
    url: jdbc:postgresql://localhost:5432/test_DB
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
